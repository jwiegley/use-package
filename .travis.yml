language: generic
dist: trusty
addons:
  apt:
    packages:
      - emacs
install:
  - curl -fsSL https://raw.githubusercontent.com/cask/cask/master/go | python
  - export PATH=/home/travis/.cask/bin:$PATH
  - cask install
script: cask exec ert-runner -l use-package.el
